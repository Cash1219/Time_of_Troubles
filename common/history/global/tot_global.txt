GLOBAL = {
	c:FRA = {
		add_treasury = {
			value = gold_reserves_limit
			multiply = -0.5
		}
	}
	
	every_country = {
		if = {
			limit = { NOT = { has_journal_entry = je_kizuna_gui } }
			add_journal_entry = { type = je_kizuna_gui }
		}
		if = {
			limit = { NOT = { has_journal_entry = je_tot_character_growth_gui } }
			add_journal_entry = { type = je_tot_character_growth_gui }
		}
		if = {
			limit = { NOT = { has_journal_entry = je_tot_band_gui } }
			add_journal_entry = { type = je_tot_band_gui }
		}
		tot_setup_default_kon_band_effect = yes
		tot_setup_default_story_bands_effect = yes
	}
}
